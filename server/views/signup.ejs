<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Sign up - Jarvis</title>

  <!-- Bootstrap & Font Awesome -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/js/all.min.js"></script>

  <style>
    body {
      background-color: #0d0d0d;
      color: #e0e0e0;
      font-family: 'Arial', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      text-align: center;
      overflow: hidden;
    }

    .auth-container {
      width: 100%;
      max-width: 400px;
      background: rgba(34, 34, 34, 0.95);
      padding: 30px 25px;
      border-radius: 16px;
      box-shadow: 0 0 30px rgba(0, 191, 255, 0.2);
      animation: fadeInUp 0.6s ease;
      transition: all 0.3s ease;
    }

    .auth-container:hover {
      box-shadow: 0 0 40px rgba(0, 191, 255, 0.4);
      transform: scale(1.01);
    }

    @keyframes fadeInUp {
      0% { opacity: 0; transform: translateY(20px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    h2 {
      color: #00bfff;
      text-shadow: 0 0 8px #00bfff;
      font-weight: bold;
      margin-bottom: 20px;
    }

    .oauth-btn {
      border: 1px solid #ddd;
      font-weight: 500;
      border-radius: 8px;
      padding: 10px;
      font-size: 16px;
      transition: background 0.3s ease, box-shadow 0.3s ease;
    }

    .oauth-btn img {
      width: 20px;
      height: 20px;
    }

    .google-btn {
      background: #fff;
      color: #444;
    }

    .google-btn:hover {
      background: #f7f7f7;
    }

    .github-btn {
      background: #24292e;
      color: white;
    }

    .github-btn:hover {
      background: #1b1f23;
    }

    .logo {
      width: 70px;
      margin-bottom: 15px;
      animation: pulse 2s infinite alternate;
    }

    @keyframes pulse {
      from { transform: scale(1); opacity: 0.8; }
      to { transform: scale(1.05); opacity: 1; }
    }

    .stars {
      position: absolute;
      width: 100%;
      height: 100%;
      background: radial-gradient(ellipse at bottom, #1b2735 0%, #090a0f 100%);
      overflow: hidden;
      z-index: -1;
    }

    .stars::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url('https://media.giphy.com/media/26AHqZycSplGWWPAI/giphy.gif');
      opacity: 0.08;
      z-index: -1;
    }

    .btn-wrapper {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .facebook-btn {
  background: #1877f2;
  color: white;
}

.facebook-btn:hover {
  background: #145dbf;
}
.instagram-btn {
  background: linear-gradient(45deg, #f58529, #dd2a7b, #8134af, #515bd4);
  color: white;
  border: none;
  box-shadow: 0 4px 12px rgba(221, 42, 123, 0.4);
  transition: all 0.3s ease-in-out;
}

.instagram-btn:hover {
  transform: scale(1.03);
  box-shadow: 0 6px 18px rgba(221, 42, 123, 0.6);
}


  </style>
</head>
<body>

  <div class="stars"></div>

  <div class="auth-container">
    <img src="/public/imgs/logo.png" alt="Jarvis Logo" class="logo" onerror="this.style.display='none'">
    <h2>Sign up to Jarvis</h2>

    <div class="btn-wrapper mt-4">
      <!-- Google OAuth -->
      <a href="/auth/google" class="btn oauth-btn google-btn w-100 d-flex align-items-center justify-content-center gap-2">
        <img src="https://developers.google.com/identity/images/g-logo.png" alt="Google logo">
        Continue with Google
      </a>

      <!-- GitHub OAuth -->
      <a href="/auth/github" class="btn oauth-btn github-btn w-100 d-flex align-items-center justify-content-center gap-2">
        <i class="fab fa-github"></i>
        Continue with GitHub
      </a>
      
      <!-- Facebook OAuth -->
  <a href="/auth/facebook" class="btn oauth-btn facebook-btn w-100 d-flex align-items-center justify-content-center gap-2">
    <i class="fab fa-facebook-f"></i>
    Continue with Facebook
  </a>
  
  <!-- Instagram OAuth -->
<a href="/auth/instagram" class="btn oauth-btn instagram-btn w-100 d-flex align-items-center justify-content-center gap-2">
  <i class="fab fa-instagram"></i>
  Continue with Instagram
</a>

    </div>
  </div>

</body>
</html>
