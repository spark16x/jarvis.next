export default function ChatInput({className}) {
  return (<div className={className} > 
  <input type="text" placeholder="Type to chat" className=" w-full absolute bottom-[20px]" ></input>
  </div>)
}